<?xml version="1.0" encoding="utf-8"?>
<!-- Комментарий должен быть вне тега -->
<!-- package="com.tazar.android" -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <!-- Разрешения для приложения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TazarApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TazarAndroid"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true">

        <!-- API ключ для Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- Экран загрузки -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TazarAndroid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Основные активности приложения -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.TazarAndroid.NoActionBar" />
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.TazarAndroid.NoActionBar" />
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.TazarAndroid.NoActionBar" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.reports.ReportTrashActivity" />
        <activity android:name=".ui.collection.CollectionPointDetailsActivity" />
        <activity android:name=".ui.collection.AddCollectionPointActivity" />
        <activity android:name=".ui.achievements.AchievementsActivity" />
        <activity android:name=".ui.ecotips.EcoTipsActivity" />
        <activity android:name=".ui.events.CleanupEventsActivity" />
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.TazarAndroid.NoActionBar" />
        <activity
            android:name=".ui.activities.CreateReportActivity"
            android:exported="false"
            android:label="@string/create_report"
            android:parentActivityName=".ui.activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>

        <!-- Провайдер для доступа к файлам приложения -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".services.NotificationService"
            android:exported="false" />

        <service
            android:name=".services.PointsCheckingService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest> 